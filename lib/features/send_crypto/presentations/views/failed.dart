import 'package:flutter/material.dart';

class SendCryptoFailed extends StatelessWidget {
  const SendCryptoFailed({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
