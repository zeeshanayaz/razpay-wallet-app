import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class SizedBoxH10 extends StatelessWidget {
  const SizedBoxH10({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 10.h,
    );
  }
}

class SizedBoxH5 extends StatelessWidget {
  const SizedBoxH5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 5.h,
    );
  }
}

class SizedBoxH15 extends StatelessWidget {
  const SizedBoxH15({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 15.h,
    );
  }
}

class SizedBoxH20 extends StatelessWidget {
  const SizedBoxH20({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 20.h,
    );
  }
}

class SizedBoxH25 extends StatelessWidget {
  const SizedBoxH25({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 25.h,
    );
  }
}

class SizedBoxH30 extends StatelessWidget {
  const SizedBoxH30({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 30.h,
    );
  }
}

class SizedBoxH40 extends StatelessWidget {
  const SizedBoxH40({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 40.h,
    );
  }
}

class SizedBoxW15 extends StatelessWidget {
  const SizedBoxW15({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 15.h,
    );
  }
}

class SizedBoxW5 extends StatelessWidget {
  const SizedBoxW5({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 5.w,
    );
  }
}

class SizedBoxW10 extends StatelessWidget {
  const SizedBoxW10({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 10.w,
    );
  }
}

class SizedBoxW20 extends StatelessWidget {
  const SizedBoxW20({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 20.w,
    );
  }
}

class SizedBoxW30 extends StatelessWidget {
  const SizedBoxW30({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 30.w,
    );
  }
}

class SizedBoxW40 extends StatelessWidget {
  const SizedBoxW40({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 40.w,
    );
  }
}
